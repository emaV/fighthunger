CREATE TABLE ec_exact (
  txnid int(11) NOT NULL default '0',
  pid varchar(30) NOT NULL default '0',
  amount decimal(10,2) NOT NULL default '0.00',
  record text NOT NULL,
  PRIMARY KEY  (pid)
) TYPE=MyISAM;